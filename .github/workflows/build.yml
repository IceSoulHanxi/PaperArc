name: Build

on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Setup Gradle cache
        uses: gradle/actions/setup-gradle@v3

      - name: Fetch arclight spigot-deobf
        run: |
          mkdir -p vendor/maven-flat
          curl -fsSL -o vendor/maven-flat/spigot-1.21.1-deobf.jar \
            https://github.com/IceSoulHanxi/PaperArc/releases/download/arclight-deps/spigot-1.21.1-deobf.jar

      - name: Build
        run: ./gradlew build --no-daemon

      - name: Collect artifacts
        run: |
          mkdir -p out
          for m in fabric neoforge forge; do
            cp "$m"/build/libs/paperarc-*[0-9].jar out/
            ls -l out/
          done

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: PaperArc-${{ github.sha }}
          path: out/*.jar
          if-no-files-found: error

