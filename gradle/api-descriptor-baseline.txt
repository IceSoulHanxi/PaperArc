# check-api-descriptors.py 的已知无害条目（A6/X-2 建立，B6-1 接入 main）。
# 一行一条 `KIND what`。只放"故意提前实现的下个版本方法"——paper-api 1.21.1 没有、
# 更高版本才加的声明，多出来不影响任何按 1.21.1 paper-api 编译的插件。
# 真正的描述符写错（同名同形参个数、类型不同）与"调用点在运行时根本不存在"必须修，
# 不许进基线：B6-1 抓出的 Server 四个 paper default 重载就是这么修的。
